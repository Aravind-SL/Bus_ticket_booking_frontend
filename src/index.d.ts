
type Station = {
  stationName: string,
  stationId: number,
  state: string
};

type Bus = {
  busNumber: number,
  busId: number,
};

type Route = {
 routeId: number,
 fromStation: number,
 toStation:  number,
 distance: number
}

